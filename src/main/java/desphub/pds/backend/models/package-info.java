@FilterDef(
        name = "officeFilter",
        parameters = @ParamDef(name = "officeId", type = Long.class)
)
package desphub.pds.backend.models;

import org.hibernate.annotations.FilterDef;
import org.hibernate.annotations.ParamDef;
